# Local environment variables (do not commit secrets)
# Next.js automatically loads `.env` for local development.
#
# This project currently uses localStorage for auth/data, so no secrets are required yet.
# Add variables here when you integrate a real backend/database.

# Example placeholders:
# NEXT_PUBLIC_APP_NAME="Ramlila Pedhewale Factory"
# NEXT_PUBLIC_BASE_URL="http://localhost:3000"

