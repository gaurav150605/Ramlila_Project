# Render Blueprint - connect this repo in Render to auto-configure the Web Service
# See: https://docs.render.com/blueprint-spec
services:
  - type: web
    name: ramlila-factory
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
